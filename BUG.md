根据您的要求，现将报告中**所有已编号的问题**（P0 级 16 项、P1 级 34 项）按统一格式列出。  
P2 和 P3 级问题因原报告未逐项编号且内容为类别概览，此处不再逐一展开，详见报告原文的“P2/P3 概览”部分。

---

## P0 致命问题（16 项）




### P0-A6
**问题名称（编号）**：P0-A6 God 模块/类（超大文件）  
**问题位置**：`app/view/bias_interface.py`（2858 行）、`app/view/widgets/freq_analyzer/corpus_store.py`（1079 行）  
**解决方法**：拆分 `bias_interface.py` 为 `bias/{dialogs, workers, filters, main}.py`；拆分 `CorpusStore` 为 `DocumentsStore`、`CleanCacheStore`、`FtsSearcher`。


---

### P0-AC3
**问题名称（编号）**：P0-AC3 缺少 PMW（每百万词频次）归一化  
**问题位置**：`freq_engine.py:654-666`、`836-850`  
**解决方法**：增加 PMW = freq / total * 1_000_000 列，并在 UI 提供切换显示选项。

---

### P0-AC4
**问题名称（编号）**：P0-AC4 英文无词形还原/词干提取  
**问题位置**：`freq_engine.py:563-580`、`concordance_engine.py`  
**解决方法**：集成 NLTK `WordNetLemmatizer` 或 spaCy，使 `running/runs/ran` 归一为同一词条。

---

## P1 重要问题（34 项）

### P1-S1
**问题名称（编号）**：P1-S1 `LICENSE_SECRET` 默认值无生产保护  
**问题位置**：`setting.py:60`  
**解决方法**：增加强制环境变量检查，若未设置则抛出异常，避免使用默认值。

---

### P1-S2
**问题名称（编号）**：P1-S2 客户端 HMAC 验签可被绕过  
**问题位置**：`license.py:162-243`  
**解决方法**：改造为“首次在线校验 + 缓存最近一次验证结果”的架构，客户端不再持有验签密钥。

---

### P1-S3
**问题名称（编号）**：P1-S3 `subprocess.Popen(..., shell=True)` 注入风险  
**问题位置**：`download_card.py:474`  
**解决方法**：改为 `shell=False` 并使用列表参数传递命令。

---

### P1-S4
**问题名称（编号）**：P1-S4 文件权限未设 `0o600`  
**问题位置**：`device_id.py:51-52`、`license.py:51`  
**解决方法**：创建敏感文件时显式设置权限为 `0o600`。

---

### P1-S5
**问题名称（编号）**：P1-S5 `tasks.db` 未加密  
**问题位置**：数据库文件  
**解决方法**：迁移至 SQLCipher 或使用 `sqlcipher` 编译版本。

---

### P1-S6
**问题名称（编号）**：P1-S6 文件名校验缺失（路径遍历风险）  
**问题位置**：`corpus_store.py:330-345`  
**解决方法**：对用户输入的文件名进行规范化（`os.path.normpath`）并检查是否包含 `..` 或绝对路径，实施白名单校验。

---

### P1-S7
**问题名称（编号）**：P1-S7 下载内容无完整性校验  
**问题位置**：下载模块  
**解决方法**：增加 SHA-256 校验、设置文件大小上限、并对 Excel 文件进行公式注入防护（如禁用动态公式）。

---

### P1-S8
**问题名称（编号）**：P1-S8 所有 `requests.post` 未显式 `verify=True`  
**问题位置**：多处 HTTP 请求  
**解决方法**：显式设置 `verify=True`（或 `verify=证书路径`）确保 SSL 证书验证。

---

### P1-A1
**问题名称（编号）**：P1-A1 `CorpusStore` 类职责过多（God 类）  
**问题位置**：`corpus_store.py`（1079 行）  
**解决方法**：按单一职责拆分为文档存储、缓存清理、全文搜索等独立类。

---

### P1-A2
**问题名称（编号）**：P1-A2 `excludePunctuation` 字段从未读取  
**问题位置**：`freq_engine.py:474、503-505`  
**解决方法**：清理该死代码，若需要则补充使用逻辑。

---

### P1-A3
**问题名称（编号）**：P1-A3 `isRunning` 字段从未读取  
**问题位置**：`task_manager.py:39、359`  
**解决方法**：移除该未使用的字段。

---

### P1-A4
**问题名称（编号）**：P1-A4 视图直接戳私有成员 `_store` / `_currentHash`  
**问题位置**：`freq_analyzer_interface.py:583-589`  
**解决方法**：在协调器类中提供公有访问方法（getter），视图通过接口访问。

---

### P1-A5
**问题名称（编号）**：P1-A5 部分文件直接 `from loguru import logger`  
**问题位置**：18 个文件  
**解决方法**：统一改为 `from app.core.utils import logger`，以利用统一的过滤和格式化。

---

### P1-A6
**问题名称（编号）**：P1-A6 `Config` 与 `cfg` 双别名混用  
**问题位置**：多处  
**解决方法**：全局统一使用 `cfg` 作为配置实例别名。

---

### P1-A7
**问题名称（编号）**：P1-A7 模块级 `taskManager = TaskManager()` 阻塞 import  
**问题位置**：`task_manager.py:451`  
**解决方法**：改为 `getTaskManager()` 懒加载函数，延迟实例化。

---

### P1-A8
**问题名称（编号）**：P1-A8 `freq_analyzer_interface.py` 混杂多类职责  
**问题位置**：`freq_analyzer_interface.py`（665 行）  
**解决方法**：按“主面板”、“三个 QThread”、“matplotlib 初始化”分别拆分到独立模块。

---

### P1-A9
**问题名称（编号）**：P1-A9 跨视图 API fallback 模式重复 3 次  
**问题位置**：`download_card.py`  
**解决方法**：提取为公共工具函数或装饰器，统一处理 fallback 逻辑。

---

### P1-A10
**问题名称（编号）**：P1-A10 `processEvents()` 在 splash 阶段可能引发重入  
**问题位置**：`main_window.py:111-112`  
**解决方法**：改用 `QTimer.singleShot` 替代 `processEvents()` 避免事件重入。

---

### P1-A11
**问题名称（编号）**：P1-A11 SQLite 连接池泄漏（`close()` 未调用）  
**问题位置**：`task_control` 相关模块  
**解决方法**：确保每次数据库操作后显式关闭连接，或使用上下文管理器（with 语句）。

---

### P1-D1
**问题名称（编号）**：P1-D1 过多相对导入（违反绝对导入规则）  
**问题位置**：16 个文件，共 59 条相对导入  
**解决方法**：全部改为绝对导入（以 `app.` 开头），符合 CLAUDE.md 规范。

---

### P1-D2
**问题名称（编号）**：P1-D2 视图层直连 `app.core.api`（同 P0-A1）  
**问题位置**：4 个视图文件  
**解决方法**：迁移至 service 层，通过 `TaskManager` 等中间层访问。

---

### P1-D3
**问题名称（编号）**：P1-D3 四个模块间存在延迟循环依赖  
**问题位置**：`freq_analyzer_interface` ↔ `concordance_widget` ↔ `corpus_import_widget` ↔ `freq_analyzer_widget`  
**解决方法**：引入依赖倒置（如定义接口）或将相互依赖的功能提取到独立子包中。

---

### P1-D4
**问题名称（编号）**：P1-D4 业务/数据库模块错放在 `view/widgets/freq_analyzer/` 下  
**问题位置**：12 个模块  
**解决方法**：迁移至 `app/core/services/` 或 `app/core/repositories/`，保持分层清晰。

---

### P1-D5
**问题名称（编号）**：P1-D5 README.md 为空  
**问题位置**：项目根目录  
**解决方法**：补充项目简介、安装步骤、运行方式、架构概述和贡献指南。

---

### P1-D6
**问题名称（编号）**：P1-D6 `.trae/rules` 未版本化（被 `.gitignore` 忽略）  
**问题位置**：`.gitignore:101`  
**解决方法**：将 `.trae/rules` 纳入版本控制，确保规则可追溯。

---

### P1-D7
**问题名称（编号）**：P1-D7 提交规则引用的需求文档不存在  
**问题位置**：`6D-CorpusClient_需求总结_按优先级.md`（缺失）  
**解决方法**：补全该文档或更新提交规则中的引用。

---

### P1-D8
**问题名称（编号）**：P1-D8 `.trae/rules` 与 `CLAUDE.md` 互相矛盾  
**问题位置**：两处配置文件  
**解决方法**：统一规则（导入根包、资源目录、入口文件位置），确保一致性。

---

### P1-D9
**问题名称（编号）**：P1-D9 多数 commit message 不合规（英文前缀、超长）  
**问题位置**：近期 13/22 条提交  
**解决方法**：CI 增加 commit message 格式校验（如使用 conventional commits），并规定行宽 ≤72。

---

### P1-D10
**问题名称（编号）**：P1-D10 提交规则正则不匹配项目编号（如 `FR-DEP-001`）  
**问题位置**：提交检查正则 `[A-Z]+-[0-9]+`  
**解决方法**：调整为 `[A-Z]+-[A-Z]+-[0-9]+` 以匹配三段式编号。

---

### P1-AC1
**问题名称（编号）**：P1-AC1 POS 后端仅支持 jieba  
**问题位置**：词性标注模块  
**解决方法**：设计可插拔后端接口，支持 LTP、HanLP 等，并允许用户切换。

---

### P1-AC2
**问题名称（编号）**：P1-AC2 HanLP fallback 静默替换规则引擎  
**问题位置**：依赖分析模块  
**解决方法**：当 HanLP 不可用时，在 UI 中给出明确提示，并允许用户选择离线规则或 LTP。

---

### P1-AC3
**问题名称（编号）**：P1-AC3 词云渲染缺版本指纹  
**问题位置**：词云生成模块  
**解决方法**：在输出结果中记录 `wordcloud` 库版本及随机种子，确保可复现。

---

### P1-AC4
**问题名称（编号）**：P1-AC4 分句正则简陋  
**问题位置**：分句模块  
**解决方法**：使用 `nltk.tokenize.sent_tokenize` 或编写更鲁棒的正则，处理引号嵌套、缩写（Mr.）和小数点（3.14）。

---

### P1-AC5
**问题名称（编号）**：P1-AC5 matplotlib 后端管理分散（8 个模块）  
**问题位置**：多处  
**解决方法**：统一在 `matplotlib_backend.py` 中管理后端设置，其他模块导入该配置。

---

### P1-AC6
**问题名称（编号）**：P1-AC6 jieba 模型版本检测不支持其他后端  
**问题位置**：版本管理模块  
**解决方法**：扩展版本检测机制，支持 hanlp、ltp、spacy 等后端的版本记录。

---

以上即为报告中所有已编号问题的格式化列表。对于 P2（44 项）和 P3（25 项）问题，因未逐项编号且多为代码清理、命名微调、注释规范等建议，建议直接参考原报告的“P2 一般问题”和“P3 建议”部分进行按优先级处理。